/*
function
    Return
    Automatic Semicolon Insertion [No Line Terminator Allowed]
    Interruptting
*/







function generate(start, end) {
    for (let i = start; i <= end; i++) {
        if  (i ===15) { 
        return 'Interruptting';//hadi ghi smia
        }
    console.log (i);
    }
}
generate(10,20);